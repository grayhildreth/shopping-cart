<div class="productDetail">
  {{#link-to 'product' product.id}}<h3>Product: {{product.name}}</h3>{{/link-to}}
  <h3>${{product.price}}</h3>
  <img src={{product.imageUrl}} class="img-responsive">
  <button class="btn" {{action 'addToCart' product}}>Add to Cart</button>
</div>
